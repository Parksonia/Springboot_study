drop table if exists member CASCADE;
create table member
(
 id bigint generated by default as identity, // bigint ==java에서 Long
 name varchar(255),
 primary key (id)
);


insert into member(name)values('spring1');  // 데이터 넣기